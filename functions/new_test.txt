
aa <- rawexp[,as.character(env[,1])]
for(x in 1:nrow(aa)){
  pvals <- 
  for(e in c("RP","Dry_Fresh","Dry_AR","RP")){
    model <- lm(as.numeric(rawexp[1,as.character(env[,1])]) ~ as.numeric(env[,2]==e))
    anova(model)[[5]][1]
  }
}

colz <- NULL
for(x in 1:10){
  colz <- c(colz, rgb(0.9,x/10,0.9))
}
colz <- c(colz, rgb(0.9,0.9,0.9))
for(x in 1:10){
  colz <- c(colz, rgb((10-x)/10,0.9,0.9))
}